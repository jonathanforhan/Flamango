import { ComputeEngine } from "@cortex-js/compute-engine";

export class MathParser extends ComputeEngine {
  parse(expression: string | undefined) {
    return super.parse(expression);
  }
  evaulate(expression: string | undefined) {
    return super.evaulate(expression);
  }
}
